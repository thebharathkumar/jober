name: ci

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install
        run: python -m pip install -e ".[dev]"

      - name: Lint
        run: python -m ruff check src tests

      - name: Type check
        run: MYPYPATH=src python -m mypy src/bus_factor

      - name: Test with coverage
        run: python -m pytest --cov=bus_factor --cov-report=term-missing

      # The demo is the end-to-end smoke test: it must run offline and produce
      # a report on every commit. If retrieval or the harness breaks, this fails.
      - name: Demo smoke test
        run: python -m bus_factor.cli demo
